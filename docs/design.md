# 迭代记录（2026-09-20 一天内十几轮）

每一轮都是用户指出"这不对"，逐条改了行为：

- v1 只报"最接近的旧观点名字" → 用户：**只贴标签不拆论证，争论的人不会意识到自己错在哪**
- v2 拆论证但用谬误名（稻草人/举证责任）→ 用户：**这太技巧层，要从思想内部拆**
- v3 改成"地基 → 谱系 → 内部反驳"，谱系落到真实运动 → 用户：**太硬，要像视频里讲清一个哲学家那种白话**
- v4 白话讲法 → 用户：**要巨魔大佬那种降段位打服感**（高维知识 / 低维说法）
- v5 谱系要求"一群人 + 什么年代 + 后来怎么了" → 用户：**光声明不够，要讲清它怎么被取缔、为什么，让这个死因变成驳倒对方的关键**
- v6 判据写成"被取代"与"倒了"两条路（被取代更狠：连胜负都不算，只是过时）
- v7 输出改成纯自然段（原来是填表）→ 用户：**还是那个制式**
- v8 分数从"约 xx%"改成**精确到个位**，反着算的番茄指数（新鲜度 = 100 − 陈旧率）
- v9 谱系优先**近年的、网上现成且滑稽的**那一支 → 用户：**老运动等于给对方面子，要让他觉得自己滑稽**
- v10 加"**技能里不举任何具体例子**"禁令（一举例就被整套照搬，每份报告一模一样）

## 结论：这个包的形状

目标不是做学问，是识人 —— 两三个特征就敢下判断，允许错，不许含糊。
